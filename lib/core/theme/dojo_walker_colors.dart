import 'package:flutter/material.dart';

class DojoWalkerColors {
  DojoWalkerColors._();

  // ============================================================
  // BRAND
  // ============================================================

  /// Main Dojo Walker brand color.
  static const Color primary = Color(0xFFE86100);

  /// Dark brand orange.
  static const Color dark = Color(0xFFC94F00);

  /// Premium dark navy used for dashboard surfaces.
  static const Color navy = Color(0xFF111827);

  /// Deep premium orange.
  static const Color deep = Color(0xFF9E3D00);

  /// Light brand orange.
  static const Color light = Color(0xFFFFF1E8);

  /// Soft brand orange.
  static const Color soft = Color(0xFFFFDCC7);

  // ============================================================
  // NEUTRAL
  // ============================================================

  static const Color white = Color(0xFFFFFFFF);

  static const Color black = Color(0xFF171717);

  static const Color background = Color(0xFFF8F9FA);

  static const Color card = Color(0xFFFFFFFF);

  static const Color textPrimary = Color(0xFF171717);

  static const Color textSecondary = Color(0xFF666666);

  static const Color textMuted = Color(0xFF999999);

  static const Color border = Color(0xFFE5E5E5);

  // ============================================================
  // STATUS
  // ============================================================

  static const Color success = Color(0xFF22C55E);

  static const Color successLight = Color(0xFFEAF8EF);

  static const Color warning = Color(0xFFF59E0B);

  static const Color warningLight = Color(0xFFFFF7E6);

  static const Color error = Color(0xFFEF4444);

  static const Color errorLight = Color(0xFFFFECEC);

  static const Color info = Color(0xFF2196F3);

  static const Color infoLight = Color(0xFFEAF4FF);

  // ============================================================
  // LIVE WALK / MAP
  // ============================================================

  /// Actual GPS route.
  static const Color routeBlue = Color(0xFF2196F3);

  /// Current walker location.
  static const Color locationBlue = Color(0xFF1976D2);

  // ============================================================
  // SPECIAL
  // ============================================================

  static const Color premium = Color(0xFF7C3AED);

  static const Color transparent = Colors.transparent;
}
